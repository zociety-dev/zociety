.out/
.claude/

# Container secrets - NEVER commit API keys
container/secrets/*
!container/secrets/.gitkeep
.zociety.log

# Talisman generated report output (not source)
talisman_report/

# direnv personal overrides (see .envrc)
.env
